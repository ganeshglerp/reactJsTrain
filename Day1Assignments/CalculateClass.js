import SquareFunction, {sumFunction, multFunction} from "./MathOperator.js"

class CalculateClass{


    printDetails(a){
        const outVal = SquareFunction(a);
        return outVal;
    }
}

export default CalculateClass; 